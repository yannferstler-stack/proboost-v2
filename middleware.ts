/**
 * Middleware Next.js — protection mot de passe site + auth dashboard.
 * La logique est dans proxy.ts pour faciliter les tests.
 */
export { proxy as default } from './proxy'
export { config } from './proxy'
